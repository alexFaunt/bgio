import styled from 'styled-components';

import { maxPageWidth } from 'client/styles/global';

const PageContent = styled.div`
  max-width: ${maxPageWidth}px;
  margin: 0 auto;
  padding: 0 1rem;
`;

export default PageContent;
